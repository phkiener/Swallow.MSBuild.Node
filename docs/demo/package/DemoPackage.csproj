<Project Sdk="Microsoft.NET.Sdk.Razor">

  <!-- Emulate the package being a NuGet dependency: -->
  <Import Project="../../../src/Swallow.NodeForMSBuild/build/Swallow.NodeForMSBuild.props" />

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <!-- Emulate the package being a NuGet dependency: -->
  <Import Project="../../../src/Swallow.NodeForMSBuild/build/Swallow.NodeForMSBuild.targets" />

</Project>
